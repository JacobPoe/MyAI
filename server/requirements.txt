# Python 3.13

# Dependencies
accelerate>=0.26.0 # Resolve error "ImportError: Using the `Trainer` with `PyTorch` requires `accelerate>=0.26.0`"
audioop-lts # Resolve issue with deprecated pydub dependency
datasets
Flask
Flask-Cors
numpy
pillow
pydub
python-dotenv
scipy
setuptools
transformers

# Linting tools
black
pylint

# Project Modules
-e utils
-e services


services
utils

########################################################################################################################
# torch
# torchaudio
# torchvision
# Importing torch, torchaudio, and torchvision would cause compatibility issues when using CUDA on some systems.
# It's recommended to install these packages separately based on your system's configuration.
# e.g., pip3 install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu128
# See https://pytorch.org/