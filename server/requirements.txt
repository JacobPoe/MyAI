# Python 3.13

# Dependencies
accelerate>=0.26.0 # Resolve error "ImportError: Using the `Trainer` with `PyTorch` requires `accelerate>=0.26.0`"
audioop-lts # Resolve issue with deprecated pydub dependency
datasets
Flask
Flask-Cors
numpy
pillow
pydub
python-dotenv
scipy
setuptools
transformers

# Linting tools
black
pylint

# Project Modules
-e utils
-e services


services
utils

########################################################################################################################
torch
# Importing torch, torchaudio, and torchvision causes compatibility issues when using CUDA on some systems.
# If you wish to use CUDA over your CPU, please install these packages manually according to your system specifications.
# e.g., pip3 install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu128
# See https://pytorch.org/

########################################################################################################################
# System Requirements
# Run these commands from your terminal to install system dependencies.

# pip install -U nemo_toolkit["asr"]